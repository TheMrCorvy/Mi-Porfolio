var contenedor = document.querySelector('#papel');

const body = 'durpa';

function imprimir()
{
	/*fetch('Controladores/class.main.php', {
		method: 'POST',
		/*body: body
	})
	.then(res => res.json())
	.then(datos => {
		console.log('SERN now knows where the time machine is.');

		escritor(datos);
	})
	fetch('Librerias/ParadoxSpiral.php').then((res) => res.json())
	.then(response => {
		console.log(response);
	}).catch(error => console.log(error));
}

function escritor(datos)
{
	contenedor.innerHTML = '';

	for(let valor of datos)
	{
		contenedor.innerHTML += 
		`<div class="clow-card">
			<i class="${valor.ClaseIcono} fa-5x"></i>
			<div>
				<h3>${valor.Titulo}</h3>
				<p>${valor.Info}</p>
				<p>Nivel: ${valor.Nivel}.</p>
			</div>
		</div>`;
	}*/
}

imprimir();